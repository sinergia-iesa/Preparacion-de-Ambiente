name: Desplegar presentación a GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Evita despliegues encimados si hacés push varias veces seguidas
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      # --base tiene que ser "/<nombre-del-repo>/" para que funcione bajo
      # https://<usuario>.github.io/<nombre-del-repo>/ (así es como sirve
      # GitHub Pages cualquier repo de proyecto). Se arma solo a partir del
      # nombre real del repo, así que no hay que tocar esta línea si algún
      # día lo renombrás.
      - name: Compilar diapositivas
        run: npx slidev build --base "/${{ github.event.repository.name }}/"

      - name: Preparar Pages
        uses: actions/configure-pages@v5

      - name: Subir artefacto
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Desplegar a GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
